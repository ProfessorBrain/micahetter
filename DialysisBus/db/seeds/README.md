# Fixture seeds

Epic 2 will add a small deterministic seed set covering multiple states,
boundary distances, unresolved geocodes, and antimeridian behavior. Production
source data must never be committed here.
